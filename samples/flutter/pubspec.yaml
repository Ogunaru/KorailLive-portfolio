name: koraillive_showcase
description: Pure Dart domain samples extracted for KorailLive portfolio review.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'

dev_dependencies:
  test: ^1.25.0
